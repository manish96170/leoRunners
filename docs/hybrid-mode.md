# Hybrid Mode

The scheduler can use customer-owned pools, managed pools, or both. Policy selects customer-first, managed-first, customer-only, managed-only, or fallback behavior. Security policy must define which repositories and job classes may use managed capacity, especially for private data and fork pull requests.
