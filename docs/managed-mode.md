# Managed Mode

The operator runs the control plane, durable state, observability, and managed capacity pools. Customers configure GitHub integration and workload policy without managing EC2, runner registration, images, or cleanup. Tenant identity and capacity ownership are explicit state fields so the scheduler can enforce isolation.
