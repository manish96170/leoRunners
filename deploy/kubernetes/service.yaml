apiVersion: v1
kind: Service
metadata:
  name: leo-runners-webhook
  labels:
    app.kubernetes.io/name: leo-runners-controller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: leo-runners-controller
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
