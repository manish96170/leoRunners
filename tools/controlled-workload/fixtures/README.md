# Fixtures

The runner tests create temporary Git repositories so every command executes
against a detached worktree. The JSON examples document the policy boundary;
they are intentionally not tied to a live repository commit.
