# Intake Fixtures

- `clean/` contains allowlisted repository files without credential-shaped
  values.
- `secret/` contains a deliberately credential-shaped test value and must be
  rejected by the intake scanner. It is not a usable credential.
